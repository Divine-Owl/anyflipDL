/**
 * Centralized external URLs (donation, project links, etc).
 *
 * Keeping these in one place avoids stringly-typed URLs scattered across
 * the UI and makes it trivial to update them later.
 *
 * @module $lib/config/links
 */

/** Ko-fi tip page for the AnyflipDL author. */
export const KOFI_URL = 'https://ko-fi.com/divine_owl';
